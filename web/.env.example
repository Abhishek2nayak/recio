# Web app build-time config (copy to .env). Vite only exposes VITE_-prefixed vars.
VITE_API_BASE_URL=http://localhost:4000
# Google OAuth *Web* client id for "Sign in with Google" (GIS).
VITE_GOOGLE_CLIENT_ID=replace_with_your_web_client_id.apps.googleusercontent.com
